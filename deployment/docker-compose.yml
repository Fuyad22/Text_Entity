version: '3.8'
services:
  web:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../logs:/app/logs
    environment:
      - FLASK_ENV=production
